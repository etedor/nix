{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && out=$(nix flake check --all-systems 2>&1); rc=$?; echo \"$out\" | grep -v \"unknown flake output 'deploy'\"; [ $rc -eq 0 ] || exit 2",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
